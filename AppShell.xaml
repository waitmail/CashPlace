<?xml version="1.0" encoding="UTF-8" ?>
<TabbedPage x:Class="CashPlace.AppShell"
            xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:views="clr-namespace:CashPlace"
            Title="CashPlace">

    <!-- Просто перечисляем страницы -->
    <views:MainPage Title="Главная" />
    <views:LoadDataPage Title="Загрузка" />
    <views:ChecksPage Title="Чеки" />

</TabbedPage>